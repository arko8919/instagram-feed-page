{
  "name": "instagram-feed-page",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.3.2",
    "react-dom": "^16.3.2",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "sass": "sass --watch src/css/sass/App.scss:src/css/App.css",
    "deploy": "npm run build&&gh-pages -d build"
  },
  "homepage": "https://arko8919.github.io/instagram-feed-page",
  "devDependencies": {
    "gh-pages": "^1.1.0"
  }
}
